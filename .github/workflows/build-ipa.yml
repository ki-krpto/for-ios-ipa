name: Build Stoat IPA
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: List Schemes
        run: |
          xcodebuild -workspace Revolt.xcworkspace -list

      - name: Resolve Package Dependencies
        run: |
          xcodebuild -workspace Revolt.xcworkspace \
            -scheme Revolt \
            -resolvePackageDependencies \
            -scmProvider xcode

      - name: Build Unsigned Archive
        run: |
          xcodebuild archive \
            -workspace Revolt.xcworkspace \
            -scheme Revolt \
            -configuration Release \
            -destination "generic/platform=iOS" \
            -archivePath Stoat.xcarchive \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            -scmProvider xcode

      - name: Export to IPA
        run: |
          mkdir -p Payload
          cp -r Stoat.xcarchive/Products/Applications/*.app Payload/
          zip -r Stoat-Unsigned.ipa Payload

      - name: Upload Finished IPA
        uses: actions/upload-artifact@v4
        with:
          name: stoat-ios-app
          path: Stoat-Unsigned.ipa
